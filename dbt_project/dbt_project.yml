name: 'f1_data_modeling'
version: '1.0.0'
config-version: 2

profile: 'f1_data_modeling'

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]

models:
  f1_data_modeling:
    staging:
      +materialized: view
      +schema: silver
    marts:
      +materialized: table
      +schema: gold